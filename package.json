{
	"name": "my-app",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"predev": "pushd flutter_app && flutter clean && flutter build web --release && popd",
		"dev": "vite dev",
		"prebuild": "pushd flutter_app && flutter clean && flutter build web --release && popd",
		"build": "vite build",
		"preview": "vite preview"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^3.0.0",
		"@sveltejs/kit": "^2.0.0",
		"@sveltejs/vite-plugin-svelte": "^4.0.0",
		"svelte": "^5.0.0",
		"vite": "^5.0.3",
		"vite-plugin-static-copy": "^2.0.0"
	}
}
